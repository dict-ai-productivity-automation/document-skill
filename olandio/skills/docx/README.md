This folder contains a small generator to produce a research Word document about anime.

Files:
- generate_anime_research.py: Generates `output/anime_research.docx` using `python-docx`.
- requirements.txt: Lists `python-docx`.

Quick run:

```bash
python -m pip install -r requirements.txt
python generate_anime_research.py
```
